# Where contact submissions are delivered. Server-side only — never expose in client code.
CONTACT_INBOX_EMAIL=support@premiumwebagency.com
# Optional transactional email provider key (Resend, Postmark, etc.)
CONTACT_MAIL_API_KEY=
